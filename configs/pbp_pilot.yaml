schema_version: "1.0"

single_game_pilot:
  game_id: "2024020669"
  expected_season_id: "20242025"
  expected_game_type: regular_season
  expected_outcome: overtime_only
  purpose: >
    Validate raw play-by-play storage, event inventory, periods,
    coordinates and reconciliation inputs before parsing multiple games.
